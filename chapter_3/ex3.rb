# This prints out a string
puts 'I will now count my chickens:'

# This computes the values within the curly bracket
# and prints the string with the computed value.
# This adds 25 + 30 then divides by 6
puts "Hens #{25 + 30 / 6}"
# This computes the values within the curly bracket
# and prints the string with the computed value.
# This multiplies 25 by 3, divides by 4, then subtracts that number from 100
puts "Roosters #{100 - 25 * 3 % 4}"
# This prints out a string
puts 'Now I will count the eggs:'
# This prints out a number that is the result of the calculation
# This divides 4 by 2 & 1/4 first and then does all the addition and subtraction
puts 3 + 2 + 1 - 5 + 4 % 2 - 1 / 4 + 6
# This prints out a string
puts 'Is it true that 3 + 2 < 5 - 7?'
# This prints out a number that is the result of the calculation
# This does the subtraction and addition,
# then asks if 5 is less than -2 that is the boolean value false.
puts 3 + 2 < 5 - 7
# This computes the values within the curly bracket
# and prints the string with the computed value.
puts "What is 3 + 2? #{3 + 2}"
# This computes the values within the curly bracket
# and prints the string with the computed value.
puts "What is 5 - 7? #{5 - 7}"

# This prints out a string
puts 'Oh, that\'s why it\'s false.'

# This prints out a string
puts 'How about some more.'

# This computes the value, a boolean value that is the result of asking
# if 5 is greater than -2, and inserts it in the string
puts "Is it greater? #{5 > -2}"
# This computes the value, a boolean value that is the result of asking
# if 5 is greater than or equal to 2, and inserts it in the string
puts "Is it greater or equal? #{5 >= 2}"
# This computes the value, a boolean value that is the result of asking
# if 5 is less than or equal to 2, and inserts it in the string
puts "Is it less or equal? #{5 <= -2}"
